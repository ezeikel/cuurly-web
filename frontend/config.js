// This is client side config only - don't put anything in here that shouldn't be public!
export const endpoint = `http://localhost:4000`;
export const prodEndpoint = `http://api.crownd.co.uk`;
export const perPage = 4;
